# Loopback is rewritten to the Metro LAN host on a physical device.
EXPO_PUBLIC_SERVER_URL=http://localhost:3000
# Web app that owns billing settings (plan, invoices, cancellation) and hosts
# the legal pages. The More tab links here for subscription management.
EXPO_PUBLIC_APP_URL=https://app.jooling.com
