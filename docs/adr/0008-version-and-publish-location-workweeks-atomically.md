# Version and publish location workweeks atomically

Each immutable Schedule Version represents the complete workweek for one Location. Post-publication edits accumulate in a Successor Draft, the Manager reviews the combined differences, and publication replaces the visible version atomically; this favors a single comprehensible source of truth and controlled notifications over independently versioned shift fragments.
